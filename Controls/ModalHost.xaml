<UserControl x:Class="LoLHelper.Controls.ModalHost"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FontFamily="{StaticResource AppFont}" FontSize="12" Foreground="{StaticResource PrimaryText}"
             Background="#330F1720" IsTabStop="False" Focusable="False"
             KeyboardNavigation.TabNavigation="Cycle" KeyboardNavigation.ControlTabNavigation="Cycle"
             KeyboardNavigation.DirectionalNavigation="Cycle" PreviewKeyDown="Host_PreviewKeyDown"
             SizeChanged="Host_SizeChanged" AutomationProperties.Name="对话框">
    <Border x:Name="DialogPanel" Background="{StaticResource SurfaceBrush}" BorderBrush="{StaticResource ControlBorderBrush}"
            BorderThickness="1" CornerRadius="6" Margin="24" MaxWidth="440" VerticalAlignment="Center">
        <Grid>
            <Grid.RowDefinitions><RowDefinition Height="Auto" /><RowDefinition Height="*" /><RowDefinition Height="Auto" /></Grid.RowDefinitions>
            <TextBlock x:Name="DialogTitle" FontSize="16" FontWeight="SemiBold" TextWrapping="Wrap" Margin="24,24,24,16" />
            <ScrollViewer x:Name="DialogScroll" Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="24,0,24,24">
                <ContentControl x:Name="DialogContent" HorizontalContentAlignment="Stretch" IsTabStop="False" />
            </ScrollViewer>
            <Border Grid.Row="2" BorderBrush="{StaticResource LineBrush}" BorderThickness="0,1,0,0" Padding="24,16">
                <WrapPanel HorizontalAlignment="Right">
                    <Button x:Name="DismissButton" Content="关闭" IsDefault="True" Click="DismissButton_Click" />
                    <Button x:Name="ConfirmButton" Content="确认" Margin="8,0,0,0" Style="{StaticResource PrimaryButton}" Visibility="Collapsed" Click="ConfirmButton_Click" />
                </WrapPanel>
            </Border>
        </Grid>
    </Border>
</UserControl>
